// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ---------- //
// st506_wrrm //
// ---------- //

#define st506_wrrm_wrap_target 0
#define st506_wrrm_wrap 1
#define st506_wrrm_pio_version 1

static const uint16_t st506_wrrm_program_instructions[] = {
            //     .wrap_target
    0x6020, //  0: out    x, 32
    0x4020, //  1: in     x, 32
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program st506_wrrm_program = {
    .instructions = st506_wrrm_program_instructions,
    .length = 2,
    .origin = -1,
    .pio_version = st506_wrrm_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config st506_wrrm_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + st506_wrrm_wrap_target, offset + st506_wrrm_wrap);
    sm_config_set_in_pin_count(&c, 32);
    sm_config_set_in_shift(&c, 0, 1, 32);
    sm_config_set_out_pin_count(&c, 32);
    sm_config_set_out_shift(&c, 0, 1, 32);
    return c;
}

static inline void st506_wrrm_program_init(PIO pio, uint sm, uint offset) {
	pio_sm_config c = st506_wrrm_program_get_default_config(offset);
	pio_sm_init(pio, sm, offset, &c);
	pio_sm_set_enabled(pio, sm, true);
}
static inline void st506_wrrm_program_deinit(PIO pio, uint sm) {
	pio_sm_set_enabled(pio, sm, false);
}

#endif

